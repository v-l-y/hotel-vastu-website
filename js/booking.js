document.addEventListener("DOMContentLoaded",()=>{const form=document.querySelector("[data-booking-form]");if(!form)return;const today=new Date().toISOString().split("T")[0];["checkin","checkout"].forEach(id=>{const el=document.getElementById(id);if(el)el.min=today});form.addEventListener("submit",e=>{e.preventDefault();const status=form.querySelector("[data-form-status]");if(status){status.hidden=false;status.textContent="Thanks. Your details are ready, but this static form will not send until the hotel confirms its official contact or form endpoint.";}})});